# App
APP_MODULE=app.main:app
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Cache / Refresh
DATABASE_PATH=data/stockinfo.db
CACHE_TTL_HOURS=6
REFRESH_INTERVAL_HOURS=6
METADATA_TTL_DAYS=7

# Auth (optional, leer = aus)
API_KEY=

# Makefile-Theme (classic|ocean|earth|night|mono|sunset|forest|neon|shell)
MAKE_THEME=classic
