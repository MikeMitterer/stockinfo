# data/sources.yaml — normales Online-Profil mit YAML als letztem Fallback
#
# Seit T-41 tut dieses Profil, was es beschreibt: Jede der fuenf Rollen fragt
# ihre Kette der Reihe nach. Bis dahin nahm die App fuer `quotes`, `daily` und
# `fx` nur die erste einsatzbereite Quelle, und `yaml-file` am Ende der Kette
# wurde nie gefragt — im Browserlauf zu T-37 hat das eine Anleihe ohne Kurs
# gekostet.
#
# `yaml-file` steht auch in `resolvers`, und das ist kein Schmuck: Ein Papier,
# das keine Online-Quelle kennt, laesst sich sonst gar nicht erst aufnehmen —
# die Aufnahme scheitert an der Aufloesung, lange bevor jemand nach einem Kurs
# fragt.
#
# Diese Datei konfiguriert nur die Quellenketten. Die eigentlichen, vom User
# gepflegten Instrumente stehen gesammelt in genau einer Datei:
# /data/assets.yaml
#
# Die Reihenfolge ist verbindlich: Online-Quellen werden zuerst gefragt.
# `yaml-file` antwortet nur, wenn davor keine Quelle ein Ergebnis liefert.

resolvers: [openfigi, yahoo-search, yaml-file]
etf_meta:  [justetf, yfinance, yaml-file]
quotes:    [yfinance, yaml-file]
daily:     [yfinance, yaml-file]
fx:        [yfinance, yaml-file]

providers:
  openfigi:
    api_key: ${OPENFIGI_API_KEY}

  yaml-file:
    path: /data/assets.yaml
